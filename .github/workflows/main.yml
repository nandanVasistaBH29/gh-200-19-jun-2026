name: GH-200 CoHack
on: push
jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Say namaste
        run: echo "hello namaste"

      - name: Checkout src code
        uses: actions/checkout@v6

      - name: list files
        run: ls -la
  
